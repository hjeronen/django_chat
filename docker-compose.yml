version: "3.8"

services:
  redis:
    image: redis:5
    ports:
      - 6379:6379
  backend:
    container_name: django-chat
    build:
      context: ./backend
      dockerfile: Dockerfile
    depends_on:
      - redis
    ports:
      - 8000:8000